<div class="eventdescription" id="eventdescription{{ event.id }}">
	<h1>{{ event.name }}</h1>
	<h3>{{ event.datetime|date("d/m/Y | H:i") }} | {{ event.venue.name }}</h3>
	<p><strong>Featuring: </strong> 
	{% for artist in event.attendingArtists %}
		<span class="artistname">{{ artist.name }}</span>
		{% if not loop.last %}, {% endif %}
	{% endfor %}
	</p>
	<p>
	{% if event.rank > 0 %}
		This recommendation is based on {{ event.rank }} of your song plays, featuring
		artists playing at this event, and your current location. 
	{% endif %}
	</p>
</div>